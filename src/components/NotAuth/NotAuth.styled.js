import styled from "styled-components";

export const NotAuthSection = styled.div`
  padding: calc(20px + (40 - 20) * ((100vw - 390px) / (1440 - 390)));

`;

export const NotAuthTitle = styled.h1`
  margin: 10px;
  color: var(--main-text-color);
  font-size: 18px;
  font-weight: 500;
  line-height: 1;
  
`;