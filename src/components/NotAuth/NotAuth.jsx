import { Section } from "../BookTrialLesson/BookTrialLesson.styled"
import { NotAuthTitle } from "./NotAuth.styled"

export const NotAuth = () => {
    return (
        <Section>
  <NotAuthTitle>
            You are not logged in. Access to this functionality is restricted to logged-in users only.
        </NotAuthTitle>

        </Section>
      
    )

}